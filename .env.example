# Copy to .env and fill in. Wikimedia throttles or blocks generic user agents at
# volume, so this must be descriptive and carry a real contact address.
# Format: appname/version (contact)
USER_AGENT=wikimap/0.1.0 (mitchellbravo007@gmail.com)
